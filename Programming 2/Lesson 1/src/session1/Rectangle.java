package session1;

/**
 * design a class named Rectangle
 * to represent a rectangle. The class contains:
 * Two double data fields named width and height that specify
 * the width and height of the rectangle. The default values
 * are 1 for both width and height.
 *
 *  no-arg constructor that creates a default rectangle.
 *   constructor that creates a rectangle with the specified width and height.
 *  * A method named getArea() that returns the area of this rectangle.
 *  * <p> *  * A method named getPerimeter() that returns the perimeter.
 */

/*public class Rectangle {//class=data field+constrcutor+method
    double width = 1;
    double height = 1;

    public Rectangle() { //default constructor
    }

    public Rectangle(double newWidth, double height) {
        width = newWidth;
        this.height = height;


        public double getArea () {
            return double width * double this.height;
        }

        public double getPerimeter () {
            return 2 * width + 2 * this.height;
        }

    }
}*/
